# AGENTS.md

## Scope
- 이 저장소는 `FE/` 프론트엔드와 `BE/` 백엔드로 구성된다.
- 공통 규칙은 이 문서에 둔다.
- 반복 절차와 검증 체크리스트는 skill로 분리한다.

## Architecture Boundaries
- 프론트엔드의 백엔드 호출은 `FE/lib/api.ts`를 단일 진입점으로 유지한다.
- 프론트엔드 컴포넌트에는 네트워크 요청과 백엔드 계약 상세를 직접 넣지 않는다.
- 백엔드 라우터는 요청 검증과 HTTP 입출력만 담당하고, 비즈니스 로직은 `BE/services/`에 둔다.
- LangGraph 그래프 구성, 상태, 노드 로직은 `BE/agents/` 아래에 둔다.
- DB 접근 로직은 `BE/crud/`, 데이터 모델은 `BE/models/`, API 입출력 스키마는 `BE/schemas/`에 둔다.

## Modification Rules
- 사용자가 명시적으로 요청하지 않으면 코드 수정, 파일 생성, 파일 삭제를 하지 않는다.
- 변경은 요청 범위에 직접 필요한 파일로 제한한다.
- 기존 구조와 책임 경계를 유지하면서 수정하고, 구조 이동이나 이름 변경은 필요할 때만 수행한다.
- 사용자가 만든 미확인 변경이나 현재 작업과 무관한 수정은 되돌리지 않는다.
- API 계약, 상태 필드, 공용 타입을 바꾸면 영향 받는 FE/BE 경로를 함께 점검한다.
- 레거시처럼 보이는 코드나 폴더도 실제 참조 여부를 확인하기 전에는 삭제하지 않는다.

## API And Streaming Rules
- 백엔드 API 라우터 prefix는 `/api/v1`를 기준으로 유지한다.
- 헬스 체크 엔드포인트는 `/health`를 사용한다.
- 스트리밍 엔드포인트는 `text/event-stream` 계약을 유지하고, 프론트엔드 소비 방식과 함께 변경한다.
- API 엔드포인트나 요청/응답 형태를 바꾸면 `BE/routers/`, `BE/services/`, `FE/lib/api.ts`를 함께 점검한다.

## Frontend Rules
- TypeScript 타입을 유지하고, props와 API 응답 형태를 명시한다.
- 상태 관리와 화면 로직은 기존 폴더 책임에 맞춰 유지한다.
- 스타일 변경은 기존 Next.js + Tailwind 기반 구조를 따른다.
- 컴포넌트에서 직접 임의의 백엔드 fetch를 추가하지 않는다.

## Backend Rules
- Python 함수에는 타입 힌트를 유지한다.
- 외부 I/O와 네트워크 호출은 비동기 흐름을 우선한다.
- 설정값과 비밀값은 환경변수로 관리하고 코드에 하드코딩하지 않는다.
- CORS와 프론트엔드 연동 계약을 깨는 변경은 이유 없이 하지 않는다.

## Review And Fix Workflow
- 변경 전에는 관련 코드와 실제 구조를 먼저 확인한다.
- 변경 후에는 먼저 review 관점으로 영향 범위, 계약 위반, 회귀 가능성을 점검한다.
- 발견한 문제는 같은 작업 흐름에서 바로 fix하고, 수정 후 다시 검증한다.
- review 결과 이상이 없을 때만 작업을 마무리한다.

## MCP Usage Rules
- MCP가 제공하는 저장소, 문서, 외부 시스템 정보가 있으면 임의 추정보다 우선한다.
- 읽기 전용 조회와 맥락 수집에 우선 사용하고, 쓰기 작업은 범위와 대상이 명확할 때만 수행한다.
- MCP 결과가 현재 워크트리와 충돌하면 로컬 파일 상태를 기준으로 다시 확인한다.
- 외부 서비스 변경 작업은 사용자가 명시적으로 요구한 경우에만 진행한다.

## Git And Commit Rules
- 커밋 메시지는 `{emoji} {Type}: {Description}` 형식을 사용한다.
- 커밋 메시지 설명은 영어로 작성한다.
- 문서 변경은 `📝 Docs: ...`, 기능 변경은 `✨ Feat: ...`, 버그 수정은 `🐛 Fix: ...` 형식을 우선 사용한다.
- 사용자가 코드 작업을 명시적으로 요청한 경우, 각 작업 완료 후 위 규칙에 맞는 커밋을 생성한다.
- push는 사용자가 명시적으로 요청한 경우에만 진행한다.
- 기본 브랜치는 `main`으로 유지한다.
- `main`만 장기 유지 브랜치로 사용한다.
- 기능 작업 브랜치는 항상 `main`에서 분기한다.
- 기능 작업은 `feature/* -> main` PR로만 반영한다.
- CI는 관련 PR과 `main` 푸시에서 확인한다.
- 로컬 터미널에서 `git merge`로 브랜치를 직접 병합하지 않는다.
- 브랜치 병합이 필요하면 반드시 GitHub MCP로 PR을 생성하고, 그 PR 기준으로 병합 절차를 진행한다.
- 브랜치 병합이 필요하면 GitHub PR 또는 웹 UI 기반 머지를 우선하고, 다른 방식은 사용자가 명시적으로 요청한 경우에만 진행한다.
- 머지 흐름은 `feature → main` 단방향으로 유지한다.
- `main`은 merge 완료된 기능만 쌓이는 기준 브랜치로 유지한다.
- 머지 방식은 `feature → main` 기본 merge commit으로 유지한다. 필요 시에만 rebase/squash를 사용한다.
- `feature/*` 브랜치를 `main`에 병합한 뒤에는 병합 완료 여부를 확인하고 해당 로컬/원격 feature 브랜치를 삭제한다.
- 백엔드 배포는 `main` 푸시에만 실행한다.
- 배포 서버의 저장소는 `git pull` 대신 `fetch` 후 `reset --hard origin/main`으로 맞춘다. 서버 로컬 브랜치 히스토리를 기준으로 동기화하지 않는다.
- GitHub Actions 백엔드 배포는 `production` environment 기준의 secret/variable 세트를 사용한다.
- `deploy/runtime/` 아래 런타임 생성 파일은 버전 관리 대상에 포함하지 않는다.

## Naming And Comments
- Python은 `snake_case`, TypeScript는 `camelCase`를 사용한다.
- 주석은 필요한 경우에만 짧게 작성하고, 기존 팀 관례에 맞춰 한국어를 우선한다.

## Safety
- API 키, 토큰, DB 접속 정보는 커밋하지 않는다.
- `main` 브랜치에 직접 반영하는 작업은 피한다.
- 파괴적 정리나 삭제는 실제 참조 여부와 영향 범위를 확인한 뒤에만 수행한다.

## RAG 작업 인계 (2026-09-20)

### 다음 세션의 시작점
- 이번 세션에서는 인계 문서만 갱신한다. 아래 구현·운영 변경·머지를 자동으로 실행하지 말고, 사용자가 다음 세션에서 작업을 재개하면 이 기록을 기준으로 이어간다.
- 아래 PR 상태와 검증 수치는 이전 작업 세션에서 전달받은 보고다. 재개 시 로컬 변경, 현재 브랜치, 실제 PR 상태와 최신 CI를 먼저 확인하고, 이미 완료된 작업을 반복하지 않는다.
- PR #33~#38은 머지·배포 완료로 보고됐다. 내용은 PDF URL 제한, PDF 분석 대상 표시 수정, 페이지 복귀 후 취소 수정, 미사용 FE 의존성·Planner 출력 제거, 전문 공유·토큰 예산 검사·페이지 보존, 사용자별 원문 보관이다.
- PR #39: https://github.com/zmarzmar/Graduation_Project/pull/39 — RAG 백엔드, 브랜치 `feature/paper-qa-rag-backend`. 최신 보고는 테스트 101개 및 CI 통과, **아직 머지 보류**다.
- PR #39는 운영 Chroma 데이터 확인과 코드 리뷰가 남았다. CI 통과나 리뷰 코멘트 0건을 실제 코드 리뷰 완료로 간주하지 않는다.

### 유지할 방향과 현재 백엔드
- ChromaDB 제거는 보류가 아니라 RAG 도입에 따라 유지하기로 결정했다. 미사용이라는 이유로 다시 제거하지 않는다.
- Coder·Reviewer는 같은 논문 전문을 사용한다. 검색 컨텍스트로의 대체는 향후 비교 실험이며 기본 동작으로 바꾸지 않는다.
- RAG 첫 기능은 **논문 한 편 대상 Q&A와 페이지 출처 표시**다. 다음은 분석 결과의 원문 근거 연결, 그다음은 전문 대 검색 컨텍스트의 비교 평가다.
- Q&A는 로그인 전용이며 첫 질문에서 지연 색인한다. 중복 제거는 사용자별이며, 실제 저장되는 페이지 목록의 JSON을 해시한다. 사용자 간 문서는 공유하지 않는다.
- Postgres `paper_documents`가 페이지 원문의 원본이고 Chroma는 재생성 가능한 파생 색인이다. 게스트·초록 기반 분석에는 문서를 보관하지 않는다. 접근 권한은 본인의 미삭제 분석 기록과 문서 상태를 기준으로 서버에서 판정한다.
- PR #39는 작업별 `index_job_id`로 색인과 검색을 분리한다. 삭제 시 원문을 비우고 툼스톤을 남기며, 재분석은 새 문서 ID를 사용한다. 고아 청크 정리는 삭제와 앱 기동 시 실행하는 것으로 보고됐다.
- 실제 연결 종료 테스트에서는 클라이언트가 끊겨도 `/ask` 색인이 계속 진행됐다. 자동 취소된다고 설명하지 않는다. 시간 제한과 작업 상태 처리로 복구한다.
- 검색 거리 임계값은 평가상 분리되지 않아 비활성화했다. 근거 부족은 정상 응답이며, 유효한 인용이 없으면 답변을 내보내지 않는다. 작은 평가 세트 결과를 품질 보장으로 표현하지 않는다.

### PR #39 머지 전 남은 확인
- 운영 Chroma의 **마운트, 실제 데이터 저장 경로, 컬렉션과 레코드 수**를 확인한다. 기존 마운트 `/chroma/chroma`가 실제 `/data`와 달라 볼륨이 비어 있어도 컨테이너 내부에 데이터가 있을 수 있다. 확인 전에 컨테이너 재생성·볼륨 삭제를 하지 않는다. 데이터가 있으면 보존 방법부터 결정한다.
- 클라이언트 `chromadb-client==1.5.5`, 서버 `chromadb/chroma:1.5.5`의 호환 테스트는 통과한 것으로 보고됐다. 버전 API 응답만으로 실제 이미지 버전을 판단하지 않는다.
- 인용 정규화는 추가 리뷰가 필요하다. 대소문자 변환, NFKC, 단어 내부 하이픈 제거도 `A/a`, `²/2`, `alpha-beta/alphabeta`처럼 의미를 바꿀 수 있다. 이미 추가한 부호·소수점·부등호 반례 외에도 이 경우를 확인하고, 원문 일치 검증을 과장하지 않는다.
- 인용문 존재와 답변을 뒷받침하는지는 별개다. 일부 인용을 제거해도 답변에 근거 없는 문장이 남을 수 있다. `dropped_citations` 안내는 이를 검증하는 수단이 아니다.
- 삭제·기동 시에만 고아 청크를 정리하면 마지막 정리 이후 도착한 쓰기는 다음 실행까지 남는다. 접근 차단과 실제 삭제 완료를 구분하고, 주기적 재시도 등 정리 완료 방법을 검토한다. 120초 유예만으로 완전 삭제를 보장하지 않는다.

### PR 3 계획: Q&A 프론트엔드와 필요한 API 계약
- 두 선택의 추천 방향은 **BE 결과 필드 변경을 같은 PR에 포함**, **홈 결과와 마이페이지 상세에 같은 Q&A 컴포넌트 재사용**이다. 사용자 재개 요청의 범위를 확인하고 진행한다.
- PR #39 머지 후 최신 `main`에서 `feature/paper-qa-frontend`를 분기한다. 머지·배포·push 권한은 해당 세션의 사용자 지시를 따른다.
- `BE/services/agent_service.py`: 저장 함수가 커밋된 분석 기록 ID와 실제 문서 연결 여부를 반환하고, SSE `complete.result`에 `analysis_id`, `has_document`를 전달한다. PDF 입력 유무만으로 `has_document=true`를 만들지 않는다. 저장 실패 시 분석 결과는 유지하되 Q&A 불가 상태를 정확히 전달한다. 현재 저장 순서를 먼저 확인한다.
- `FE/lib/types/agent-run.ts`: 결과 필드와 질문·답변·출처 타입을 명시한다. `FE/lib/api.ts`에 `askPaper()`를 두고 200·202·409 및 인증·접근 오류를 처리한다. API는 `POST /api/v1/analyses/{analysis_id}/ask`다.
- `FE/components/agent/PaperQa.tsx`를 결과 화면과 마이페이지 상세에서 재사용한다. 질문 입력, 답변, 펼칠 수 있는 `[p.5]` 출처와 검증된 원문 구절을 제공한다. PDF 뷰어 연결·서버 대화 저장은 이번 범위 밖이다.
- Q&A 탭은 `analysis_id` 유무만으로 숨기지 않는다. PDF·선택 논문 분석 결과에서 로그인 여부 → 저장 성공 여부 → 원문 보유 여부 순으로 안내한다. 게스트는 로그인 안내, 저장 실패는 저장 실패 안내, 원문 없는 기존·초록 기반 기록은 재분석 안내를 구분한다.
- 202는 “논문 검색 준비 중”으로 표시하고 제한된 자동 재시도를 한다. `Retry-After` 헤더와 `retry_after_seconds` 중 실제 BE 계약에 맞춰 값을 읽는다. 화면 이탈·분석 변경·로그아웃 시 타이머와 요청을 정리하고 늦은 응답도 반영하지 않는다.
- 질문은 BE와 같은 500자 제한이며 요청 중 중복 제출을 막는다. `answerable=false`는 오류가 아닌 근거 부족 결과로 표시한다. `dropped_citations>0`이면 일부 출처를 확인하지 못했다고 알린다.
- 현재 API는 질문 하나만 전달하므로 **질문별 독립 답변**이다. 화면에 이전 질문을 남겨도 모델이 대화 문맥을 기억한다고 표현하지 않는다.
- 검증: 저장 성공·실패·문서 보관 실패 시 결과 계약에 대한 BE 테스트, FE lint·build, 실제 BE·Chroma를 통한 최초 색인·답변·출처·근거 없음·게스트·기존 기록·202 재시도·화면 이동 후 늦은 응답 확인. FE 테스트 프레임워크는 이 작업만을 위해 추가하지 않는다.

### 별도 작업으로 남긴 항목
- 배포 중 `/health` 지연·타임아웃 원인 조사. 자원 부족·nginx 문제는 아직 가설이다.
- OpenAI 계정 TPM 확인.
- 페이지 이동 시 초록 폴백 모달 소실, 로컬 `npm run dev`의 Tailwind 경로 오류.
- Reviewer의 관대한 통과 기준 개선과 화면의 “LLM 검토 통과” 표현 정리.
- 위 항목을 Q&A PR에 무관하게 섞지 않는다.
