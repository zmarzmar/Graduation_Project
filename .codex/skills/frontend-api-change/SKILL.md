# frontend-api-change

## Purpose
- 프론트엔드에서 API 호출, 타입, UI 상태 처리를 함께 수정할 때 사용한다.

## Checklist
- 변경 대상 API가 `FE/lib/api.ts`를 단일 진입점으로 유지하는지 확인한다.
- 요청/응답 타입과 호출 함수 시그니처를 함께 갱신한다.
- 호출 컴포넌트의 로딩, 에러, 취소 흐름을 점검한다.
- 환경변수와 API base 경로가 기존 계약과 맞는지 확인한다.
- 필요한 프론트 검증을 수행하고 남은 리스크를 기록한다.
